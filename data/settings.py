WINDOW_WIDTH, WINDOW_HEIGHT = 800, 600 # TODO: Change the window size later. How do we allow the user to change the window size? Should we allow the user to change the window size?

LAYERS = {
    'background': 0,
    'background_details': 1,
    'foreground': 2,
    'foreground_details_bottom': 3,
    'foreground_details_top': 4,
}